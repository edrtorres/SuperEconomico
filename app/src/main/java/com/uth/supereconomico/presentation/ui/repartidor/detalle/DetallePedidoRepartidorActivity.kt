package com.uth.supereconomico.presentation.ui.repartidor.detalle

import android.os.Bundle
import android.widget.Toast
import androidx.appcompat.app.AppCompatActivity

class DetallePedidoRepartidorActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        Toast.makeText(this, "Detalle Pedido (Placeholder)", Toast.LENGTH_SHORT).show()
        finish()
    }
}
